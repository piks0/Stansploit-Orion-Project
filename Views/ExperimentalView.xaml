<UserControl x:Class="StansploitOrionProject.Views.ExperimentalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel Margin="10" HorizontalAlignment="Center" Width="600">
        <Border Background="#401010" BorderBrush="#802020" BorderThickness="1" Padding="12" Margin="0,0,0,20">
            <TextBlock Text="WARNING: Use at your own risk — may cause system instability!" 
                       Foreground="#FFAAAA" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center"/>
        </Border>

        <StackPanel x:Name="TweakList" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" Margin="0,8">
                <Button Content="Apply" Tag="Nagle" Click="Tweak_Click" Margin="0,0,10,0" Padding="8,3"/>
                <Button Content="Revert" Tag="Nagle_Revert" Click="Tweak_Click" Margin="0,0,15,0" Padding="8,3"/>
                <TextBlock Text="Disable Nagle's Algorithm" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,8">
                <Button Content="Apply" Tag="HAGS" Click="Tweak_Click" Margin="0,0,10,0" Padding="8,3"/>
                <Button Content="Revert" Tag="HAGS_Revert" Click="Tweak_Click" Margin="0,0,15,0" Padding="8,3"/>
                <TextBlock Text="Hardware-Accelerated GPU Scheduling" Foreground="#FFFFFF" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <Border BorderBrush="#333333" BorderThickness="1" Margin="0,25,0,0" Background="#252526">
            <ScrollViewer Height="150">
                <TextBlock x:Name="LogOutput" Text="Awaiting input..." Margin="10" Foreground="#FFFFFF" FontSize="13"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>