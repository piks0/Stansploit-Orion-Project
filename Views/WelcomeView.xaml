<UserControl x:Class="StansploitOrionProject.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{StaticResource BackgroundColor}">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Logo / Title -->
            <TextBlock FontSize="36" FontWeight="Bold" TextAlignment="Center" Foreground="{StaticResource AccentColor}" Margin="0,0,0,15">
                <Run Text="Stansploit"/>
                <LineBreak/>
                <Run Text="Orion Project"/>
            </TextBlock>

            <!-- Tagline -->
            <TextBlock Text="Next-Generation System Optimization &amp; Gaming Utility Suite" FontSize="16" Foreground="#B0B0B0" TextAlignment="Center" Margin="0,0,0,40"/>

            <!-- Let's Go Button -->
            <Button x:Name="LetsGoButton" Content="Let's Go!" Click="LetsGo_Click" Width="200" Height="50" FontSize="18" FontWeight="Bold" Background="{StaticResource AccentColor}" Foreground="Black" HorizontalAlignment="Center">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="White" BorderThickness="1">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
