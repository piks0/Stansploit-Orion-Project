<UserControl x:Class="StansploitOrionProject.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Left" Width="600">
        <!-- System information directly on the black page background, no background card/box -->
        <StackPanel x:Name="InfoPanel" Margin="0,0,0,25">
            <TextBlock x:Name="CpuText" Text="CPU: Loading..." Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock x:Name="GpuText" Text="GPU: Loading..." Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock x:Name="RamText" Text="RAM: Loading..." Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock x:Name="OsText" Text="OS: Loading..." Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock x:Name="PowerText" Text="Power Plan: Loading..." Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,0"/>
        </StackPanel>

        <Button Content="Restore All Defaults" Click="RestoreAll_Click" Background="#800020" Foreground="White" Padding="12,8" HorizontalAlignment="Left" FontWeight="Bold"/>
    </StackPanel>
</UserControl>