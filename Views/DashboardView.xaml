<UserControl x:Class="StansploitOrionProject.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="700">
    <StackPanel Margin="0,30,0,20" HorizontalAlignment="Stretch" MaxWidth="800">
        <!-- Hardware Specifications Card -->
        <TextBlock Text="HARDWARE" Foreground="{StaticResource MutedTextColor}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Left"/>
        <Border Background="{StaticResource PanelBackground}" CornerRadius="20" Padding="30" Margin="0,0,0,40" HorizontalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Stretch">
                <TextBlock x:Name="StaticCpuText" Text="CPU: Loading..." Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,12" HorizontalAlignment="Left"/>
                <TextBlock x:Name="StaticGpuText" Text="GPU: Loading..." Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,12" HorizontalAlignment="Left"/>
                <TextBlock x:Name="StaticRamText" Text="Total RAM: Loading..." Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,12" HorizontalAlignment="Left"/>
                <TextBlock x:Name="StaticOsText" Text="Operating System: Loading..." Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <!-- System Status & Metrics Card -->
        <TextBlock Text="SYSTEM METRICS" Foreground="{StaticResource MutedTextColor}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Left"/>
        <Border Background="{StaticResource PanelBackground}" CornerRadius="20" Padding="30" Margin="0,0,0,40" HorizontalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Stretch">
                <TextBlock x:Name="CpuUsageText" Text="CPU: 0.0%" Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="GpuUsageText" Text="GPU: N/A" Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="RamUsageText" Text="RAM: 0.00 / 0.00 GB" Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="DiskUsageText" Text="Disk: 0.0%" Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="NetUsageText" Text="Network: 0.0 KB/s" Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="UptimeText" Text="Uptime: 00:00:00" Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="PowerText" Text="Power: Loading..." Foreground="{StaticResource TextColor}" FontSize="16" Margin="0,0,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <!-- Quick Actions Card -->
        <TextBlock Text="QUICK ACTIONS" Foreground="{StaticResource MutedTextColor}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Left"/>
        <Border Background="{StaticResource PanelBackground}" CornerRadius="20" Padding="30" Margin="0,0,0,10" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Tweaks" Click="GoToTweaks_Click" Margin="0,0,20,0" FontSize="16" Foreground="{StaticResource AccentColor}"/>
                <Button Content="Installer" Click="GoToInstaller_Click" Margin="0,0,20,0" FontSize="16" Foreground="{StaticResource AccentColor}"/>
                <Button Content="Task Manager" Click="OpenTaskManager_Click" Margin="0,0,0,0" FontSize="16" Foreground="{StaticResource AccentColor}"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>


