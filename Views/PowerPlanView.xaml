<UserControl x:Class="StansploitOrionProject.Views.PowerPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Left" Width="600">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Left">
            <TextBlock Text="Current Power Plan: " Foreground="#FFFFFF" FontSize="14" FontWeight="SemiBold"/>
            <TextBlock x:Name="CurrentPlanText" Text="Unknown" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,20" HorizontalAlignment="Left">
            <Button Content="Apply Orion Gaming Plan" Click="ApplyPlan_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Restore Defaults" Click="RestoreDefaults_Click" Padding="10,5"/>
        </StackPanel>

        <Border BorderBrush="#333333" BorderThickness="1" Background="#252526">
            <ScrollViewer Height="200">
                <TextBlock x:Name="LogOutput" Text="Log initialized..." Margin="10" Foreground="#FFFFFF" FontSize="13"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>