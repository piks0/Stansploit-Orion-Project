<UserControl x:Class="StansploitOrionProject.Views.InstallerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Left" Width="600">
        <StackPanel x:Name="PackageList" Margin="0,0,0,20">
            <CheckBox Content="MSI Afterburner" Tag="MSI.Afterburner" Foreground="#FFFFFF" FontSize="14" Margin="0,5"/>
            <CheckBox Content="RivaTuner Statistics Server" Tag="Guru3D.RTSS" Foreground="#FFFFFF" FontSize="14" Margin="0,5"/>
            <CheckBox Content="Display Driver Uninstaller (DDU)" Tag="WagnardSoft.DDU" Foreground="#FFFFFF" FontSize="14" Margin="0,5"/>
            <CheckBox Content="7-Zip" Tag="7zip.7zip" Foreground="#FFFFFF" FontSize="14" Margin="0,5"/>
            <CheckBox Content="Discord" Tag="Discord.Discord" Foreground="#FFFFFF" FontSize="14" Margin="0,5"/>
            <CheckBox Content="Steam" Tag="Valve.Steam" Foreground="#FFFFFF" FontSize="14" Margin="0,5"/>
        </StackPanel>

        <Button Content="Install Selected" Click="Install_Click" Padding="10,5" Width="150" HorizontalAlignment="Left"/>

        <Border BorderBrush="#333333" BorderThickness="1" Margin="0,20,0,0" Background="#252526">
            <ScrollViewer Height="150">
                <TextBlock x:Name="LogOutput" Text="Waiting for selection..." Margin="10" Foreground="#FFFFFF" FontSize="13"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>