<UserControl x:Class="StansploitOrionProject.Views.DebloaterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Left" Width="650">
        <ScrollViewer Height="280">
            <StackPanel>
                <GroupBox Header="Bloatware Apps (PowerShell)" Foreground="#E100FF" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10">
                    <StackPanel x:Name="AppList" Margin="5">
                        <CheckBox Content="Xbox App Extras" Tag="Microsoft.XboxApp" Foreground="#FFFFFF" FontSize="13" Margin="0,4"/>
                        <CheckBox Content="Cortana" Tag="Microsoft.549981C3F5F10" Foreground="#FFFFFF" FontSize="13" Margin="0,4"/>
                        <CheckBox Content="OneDrive" Tag="OneDrive" Foreground="#FFFFFF" FontSize="13" Margin="0,4"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Unnecessary Services (SC)" Foreground="#E100FF" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10">
                    <StackPanel x:Name="ServiceList" Margin="5">
                        <CheckBox Content="Windows Search Indexing" Tag="WSearch" Foreground="#FFFFFF" FontSize="13" Margin="0,4"/>
                        <CheckBox Content="SysMain (Superfetch)" Tag="SysMain" Foreground="#FFFFFF" FontSize="13" Margin="0,4"/>
                        <CheckBox Content="Connected Telemetry" Tag="DiagTrack" Foreground="#FFFFFF" FontSize="13" Margin="0,4"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" Margin="0,15,0,10" HorizontalAlignment="Left">
            <Button Content="Select All Safe" Click="SelectSafe_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Apply Selected" Click="Apply_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Restore Defaults" Click="Restore_Click" Padding="10,5"/>
        </StackPanel>

        <Border BorderBrush="#333333" BorderThickness="1" Background="#252526">
            <ScrollViewer Height="90">
                <TextBlock x:Name="LogOutput" Text="Ready..." Margin="10" Foreground="#FFFFFF" FontSize="13"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>